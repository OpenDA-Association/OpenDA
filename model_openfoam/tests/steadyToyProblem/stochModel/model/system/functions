/*--------------------------------*4D-COOL*----------------------------------*\ 
| -----------------------------OpenFOAM v 1712 -------------------------------| 
\*--------------------------------*4D-COOL*----------------------------------*/ 
FoamFile 
{
version    2.0; 
format     ascii; 
class      dictionary; 
object     functions; 
} 
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * // 

functions
{
residuals
{
    type        residuals;
    functionObjectLibs ("libutilityFunctionObjects.so");
    writeControl    timeStep;
    writeInterval   1;
    fields        (U p_rgh k omega T        );
}
probe_Assimilatie_T
{
    type    probes;
    libs    ("libsampling.so");
    writeControl    timeStep;
    writeInterval    1;
    fields    (T );
    probeLocations
    (
        (2.4 3.0 4.5)
        (4.8 0.9 4.5)
        (4.8 3.0 4.5)
        (4.8 5.4 4.5)

    );

}
}

// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * // 
